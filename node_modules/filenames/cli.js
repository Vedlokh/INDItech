#!/usr/bin/env node
const filename = require("./filenames.js");

const run = () => {
  if (process.argv.length < 3) {
    console.warn("[usage]: rename [directory_path]");
    return;
  }

  const dir = process.argv[2];

  console.log(filename(dir));
};

run();
